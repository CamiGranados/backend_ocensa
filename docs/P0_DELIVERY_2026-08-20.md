# Entrega P0 / Fase 2 reconstruida — 2026-08-20

## Resultado

Checkpoint fail-closed preparado en `codex/thps-p0-safety-contracts` como rama
de revisión. No se hizo migración, carga de datos, despliegue ni cambio sobre
`main`.

## Implementado

- Configuración versionada sin cadena SQL; flags de persistencia y publicación
  bloqueados en `false`, y analítica heredada cerrada sin toggle.
- `bin/`, `obj/` y extractos tabulares retirados del índice y cubiertos por
  `.gitignore`.
- Importador heredado retirado con 410, sin parser ni ruta de escritura.
- Endpoint versionado de preflight con límites de transporte, temporal
  `DeleteOnClose`, SHA-256, validación ZIP y todas las hojas.
- Modelo raw no destructivo, casos de censura y guardas de linaje.
- Identidades idempotentes que incluyen versión de esquema y clasificador.
- DTO de lote/release bloqueado y respuesta raíz compatible con el frontend.
- Middleware 503 para impedir cifras legacy sin release.
- EF Core alineado a 8.0.30 y dependencia OpenAPI no utilizada eliminada.
- Proyecto de pruebas unitarias/contractuales y workflow de CI.
- Chequeo reproducible de artefactos versionados y patrones de secretos.

## Evidencia ejecutada en este entorno

- `git diff --check`: sin errores de whitespace.
- `.github/scripts/check-tracked-safety.sh`: aprobado.
- Búsqueda estática: el endpoint versionado no referencia `AppDbContext` ni
  `SaveChanges`; no queda cadena operacional en archivos versionados activos.

El saneamiento de este árbol no borra objetos de commits anteriores. El historial
remoto puede conservar la antigua configuración y el XLSX; debe mantenerse el
repositorio privado y planificar una reescritura coordinada del historial si el
propietario determina que esos objetos no deben conservarse.

## Evidencia no ejecutada

Este entorno no contiene `dotnet`, `csc` ni SQL Server. Por tanto, todavía no se
puede afirmar que restore/build/test o la integración SQL hayan pasado. La CI
debe producir la primera evidencia ejecutable después de publicar la rama.

## Bloqueos antes de UAT

1. CI verde con SDK 8.0.408.
2. Revisión de dependencias y alertas de seguridad.
3. SQL Server aislado con migración raw/release todavía por crear.
4. Pruebas transaccionales, concurrentes y de rollback.
5. Autenticación/autorización.
6. Contrato científico y aprobación de métricas/release.
7. UAT frontend-backend con el XLSX vigente y sin activar flags antes de tiempo.

## Estado de decisión

**Condicionado — NO UAT — NO producción.** El comportamiento correcto por ahora
es 410 para el importador viejo, 503 para preflight válido y 503 para analítica
legacy. Ninguna respuesta 200 puede representar datos nuevos o publicados.
